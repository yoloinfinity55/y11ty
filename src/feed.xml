<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>Y11ty Blog</title>
    <subtitle>A modern blog built with Eleventy & Tailwind CSS</subtitle>
    <link href="{{ '/' | url | absoluteUrl }}" rel="self"/>
    <link href="{{ '/' | url | absoluteUrl }}"/>
    <updated>{{ collections.posts | rssLastUpdatedDate }}</updated>
    <id>{{ '/' | url | absoluteUrl }}</id>
    <author>
        <name>Y11ty Blog</name>
    </author>
    {% for post in collections.posts %}
    <entry>
        <title>{{ post.data.title }}</title>
        <link href="{{ post.url | url | absoluteUrl }}"/>
        <updated>{{ post.date | rssDate }}</updated>
        <id>{{ post.url | url | absoluteUrl }}</id>
        <content type="html">
            <![CDATA[
                {% if post.data.description %}
                <p><strong>{{ post.data.description }}</strong></p>
                {% endif %}
                {{ post.templateContent | striptags | truncate: 500 }}
            ]]>
        </content>
    </entry>
    {% endfor %}
</feed>
